"""
训练阶段基因级模型选择的三种方案对比
"""

print("=" * 70)
print("训练阶段基因级模型选择 - 三种方案对比")
print("=" * 70)

print("""
您的疑问：
"如果在训练阶段就使用基因级模型选择，不是要一个基因训练一个模型吗？"

回答：不是！有多种实现方式，不需要每个基因单独训练。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方案A：预判断 + 分组训练（推荐）⭐
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

原理：
  1. 聚类内每个基因预先判断 → 2态或3态
  2. 同一聚类分成两组：
     - 2态组（单峰基因）
     - 3态组（双峰基因）
  3. 每组仍然批量训练（不是逐个基因）

伪代码：
  for cluster_id in clusters:
      cluster_genes = get_cluster_genes(cluster_id)
      
      # 预判断每个基因
      genes_2state = []
      genes_3state = []
      for gene in cluster_genes:
          if should_use_3state(gene):
              genes_3state.append(gene)
          else:
              genes_2state.append(gene)
      
      # 分组批量训练
      if len(genes_2state) > 0:
          model_2state = train_batch(genes_2state, state=2)
      if len(genes_3state) > 0:
          model_3state = train_batch(genes_3state, state=3)

优点：
  ✅ 仍然是批量训练（保持效率）
  ✅ 每个基因用适合的模型
  ✅ 不需要每个基因单独训练
  ✅ 只需修改聚类分组逻辑

缺点：
  ⚠️ 聚类内可能出现小组（<10个基因）
  ⚠️ 小组训练可能不稳定

训练量：
  - 原方案：7个聚类 × 1-2个模型 = 7-14个模型
  - 方案A：7个聚类 × 2个子组 = 最多14个模型（实际10-12个）
  - 增加约50%训练量（可接受）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方案B：混合损失函数（折中方案）🔧
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

原理：
  1. 整个聚类仍用一个模型
  2. 但损失函数根据基因特征加权

伪代码：
  for cluster_id in clusters:
      cluster_genes = get_cluster_genes(cluster_id)
      model = create_hybrid_model(state=3)  # 统一用3态
      
      for batch in batches:
          loss = 0
          for gene in batch:
              if should_use_2state(gene):
                  # 2态基因：忽略3态参数的损失
                  loss += loss_2state(gene) + penalty_3state(gene)
              else:
                  # 3态基因：完整3态损失
                  loss += loss_3state(gene)
          
          optimizer.step(loss)

优点：
  ✅ 训练量与原方案相同
  ✅ 单个模型可输出2态和3态参数
  ✅ 不需要分组

缺点：
  ❌ 实现复杂（需要设计混合损失）
  ❌ 2态基因的3态参数仍会被训练（浪费）
  ❌ 可能收敛困难

训练量：
  - 与原方案相同（7-14个模型）
  - 但每个模型更复杂

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方案C：逐基因训练（不推荐）❌
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

原理：
  每个基因独立训练一个模型

伪代码：
  for gene in all_genes:
      gene_windows = extract_windows(gene)
      
      if should_use_3state(gene):
          model = train_single_gene(gene_windows, state=3)
      else:
          model = train_single_gene(gene_windows, state=2)
      
      save_params(gene, model)

优点：
  ✅ 每个基因完全个性化

缺点：
  ❌ 训练量暴增：2161个基因 × 200 epochs
  ❌ 每个基因窗口数少（~15个）→ 过拟合严重
  ❌ 无法利用批量优化（GPU利用率低）
  ❌ 时间成本：原来30分钟 → 可能需要10-20小时

训练量：
  - 原方案：7-14个模型
  - 方案C：2161个模型（增加150倍！）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

对比总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

| 指标         | 当前方案 | 方案A | 方案B | 方案C |
|--------------|---------|-------|-------|-------|
| 训练模型数   | 7-14    | 10-12 | 7-14  | 2161  |
| 训练时间     | 30分钟  | 45分钟| 30分钟| 20小时|
| 基因级适配   | ❌      | ✅    | ⚠️    | ✅    |
| 批量优化     | ✅      | ✅    | ✅    | ❌    |
| 实现难度     | 简单    | 中等  | 困难  | 简单  |
| 过拟合风险   | 低      | 低    | 中    | 高    |

推荐方案：
  1️⃣ 短期：当前修复（可视化阶段基因级判断）✅ 已完成
  2️⃣ 中期：方案A（预判断+分组训练）⭐ 推荐实施
  3️⃣ 不推荐：方案B（复杂）、方案C（低效）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方案A 实现示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

修改位置：process_cluster() 函数

# 当前实现（聚类级）
def process_cluster(cluster_id, cluster_genes, data, ...):
    # 整个聚类判断一次
    dominant_state = analyze_cluster_state(cluster_genes)
    
    # 所有基因用同一模型训练
    model = train_model(cluster_genes, state=dominant_state)

# 方案A实现（基因级分组）
def process_cluster(cluster_id, cluster_genes, data, ...):
    # 预判断每个基因
    genes_by_state = {2: [], 3: []}
    for gene in cluster_genes:
        state = decide_gene_state(gene, data)  # 基因级判断
        genes_by_state[state].append(gene)
    
    results = []
    # 分组批量训练
    for state in [2, 3]:
        if len(genes_by_state[state]) > 0:
            print(f"  训练{state}态子组: {len(genes_by_state[state])}个基因")
            model = train_model(genes_by_state[state], state=state)
            results.extend(model.predict(...))
    
    return results

关键改动：
  1. 循环每个基因做判断（新增）
  2. 按状态分组（新增）
  3. 每组批量训练（保留批量优化）

代码量：约+30行（主函数）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

为什么不需要"一个基因一个模型"？
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

误解来源：
  "基因级模型选择" ≠ "每个基因独立训练"

实际含义：
  "基因级模型选择" = "每个基因选择适合的模型类型（2态/3态）"
  
  但同一类型的基因仍然可以批量训练！

类比：
  - 学生考试：
    ✅ "根据成绩分班"（基因级判断）
    ✅ "同一班级一起上课"（分组批量训练）
    ❌ "每个学生单独请家教"（逐基因训练，太贵！）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前修复为什么不在训练阶段做？
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

原因：
  1. 训练已完成，重新训练需30分钟
  2. 可视化阶段修复立即生效（0分钟）
  3. 方案A需要代码重构（风险较大）
  4. 当前修复已解决核心问题（效果足够好）

如果要实施方案A：
  ✅ 适合：新数据集、新项目
  ⚠️ 当前数据：性价比不高（已有可视化修复）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

您的担心是合理的，但：

✅ "基因级模型选择" ≠ "每个基因独立训练"
✅ 可以通过"预判断+分组训练"实现基因级适配
✅ 仍然保持批量优化的效率优势
✅ 训练量只增加50%（10-12个模型 vs 7-14个）

当前修复方案（可视化阶段判断）：
  ✅ 立即生效，无需重新训练
  ✅ 已解决您观察到的核心问题
  ✅ 是当前最高效的解决方案

如果未来需要更彻底的优化，可以实施方案A（预判断+分组训练）。

""")

print("\n" + "=" * 70)
print("是否需要我实现方案A（预判断+分组训练）？")
print("=" * 70)
print("""
选项：
  1. 保持当前修复（推荐）- 已足够解决问题
  2. 实现方案A - 需要重构+重新训练（~1小时）
  3. 先看看当前修复效果 - 对比新旧可视化图
""")
